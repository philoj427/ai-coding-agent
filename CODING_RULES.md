# Coding Rules

This project follows the V1.35 design:

- Patch-based editing only.
- Search blocks must match exactly one location.
- Keep changes scoped to the target file.
- Do not commit automatically.
- Validate with tests before review.

When generating patches, prefer the smallest possible change that satisfies the task.
